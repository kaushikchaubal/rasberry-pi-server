apply plugin: 'java'
apply plugin:'application'

sourceCompatibility = 1.8
version = '1.0'

mainClassName = "app.RasberryPiServer"
applicationName = "RasberryPiServer"

repositories {
    mavenCentral()
}

dependencies {
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.0'
}	

task stage(dependsOn: ['clean', 'installApp'])

jar {
  destinationDir = file('jar')
}
